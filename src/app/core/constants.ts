/**
 * localStorage key for last path user was viewing.
 * Used for restoring session on app startup
 */
export const LAST_LOCATION_KEY = '_lastLocation';
export const SELECTED_LANGUAGE = '_selectedLanguage';
